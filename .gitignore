# dependencies / build
node_modules/
dist/
build/
.vite/
coverage/

# env / secrets — never commit real keys; the .env.example template is the exception
.env
.env.*
!.env.example
*.local

# Claude Code local-scope memory (do NOT check in)
.claude/agent-memory-local/
# Claude Code transient runtime files
.claude/scheduled_tasks.lock

# design-handoff / debug archives dropped in the repo root
*.zip

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
